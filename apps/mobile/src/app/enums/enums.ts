export enum FormatEnum {
  ForTime = 1,
  Amrap,
  Emom,
  EmomUntil,
  DeathBy,
  ForTree,
}

export enum FormatFlowEnum {
  OneWay = 1,
  Returning,
  Recursive,
  Coop,
}

export enum GaugeEnum {
  Repetitions = 1,
  Seconds,
  Meters,
}

export enum ExerciseAttributeEnum {
  Gauge = 1,
  BuyIn,
  BuyOut,
  Interval,
  Penalty,
}

export enum ModalityEnum {
  Metabolic = 1,
  Gymnastics,
  WeightLifting,
}
