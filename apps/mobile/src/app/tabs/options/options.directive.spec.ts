import { OptionsDirective } from './options.directive';

describe('OptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionsDirective();
    expect(directive).toBeTruthy();
  });
});
